<div class="bg-cover relative h-20 md:h-screen bg-no-repeat bg-center" style="background-image: url({{ post.thumbnail ? post.thumbnail.src('full') : featured_image }});">
  <h1 class="article-h1 relative top-1/3 left-0 {{ se_active ? 'md:left-1/3' : 'md:-left-1/3' }} pl-4 uppercase text-white text-4xl transform md:-rotate-90 {{ se_active ? 'bg-indigo-900' : 'bg-red-900' }}">{{post.title ? post.title : title}}</h1>
</div>
